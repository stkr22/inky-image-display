[alembic]
# Packaged migrations live inside the API source tree so they ship with the wheel.
script_location = src/inky_image_display_api/_migrations
# Overridden at runtime from API_DATABASE_PATH env var (see env.py)
sqlalchemy.url = sqlite:///./inky.db

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
