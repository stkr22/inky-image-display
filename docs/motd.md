# Message of the day (MOTD)

A daily positive story — great science, community achievements, positive
humor, everyday heroism — generated by Gemini, split into short content
parts, and shown as a takeover across selected displays.

## Content parts

One generated story decomposes into six parts, each rendered as a full
screen at every participating panel's exact resolution:

| Part | Screen |
|------|--------|
| `what` | What happened (1-2 short sentences) |
| `why` | Why it matters |
| `when` | When it happened |
| `image` | AI illustration of the story (uses the GenAI prompt presets; new configs default to the seeded `e_ink_scene` preset, whose composition handles scenes and abstract subjects — the operator can pick any other preset) |
| `qr` | QR code linking to the source article ("More details") |
| `takeaway` | The takeaway message |

Text parts can be combined two-per-screen with compound keys such as
`what+when` — useful when there are fewer displays than parts. The QR
screen is skipped automatically when the story has no source URL.

## Story sources

Configured per config (`source_mode`):

- **`grounded`** — Gemini with Google Search grounding: a real story from
  roughly the last week, with the source URL taken from grounding metadata
  (never from model text, which fabricates URLs). Two API calls: a grounded
  free-text research call, then a structuring call with a JSON schema
  (grounding and response schemas cannot be combined in one call).
- **`knowledge`** — model knowledge only: timeless/historical stories, no
  web search, no source URL (and therefore no QR screen).

The operator-editable prompt (`content_prompt`, default in
`inky_image_display_shared.motd.DEFAULT_MOTD_PROMPT`) only steers themes and
tone; the structural output contract is appended in code so edits cannot
break parsing.

## Display lifecycle

Displaying is a **takeover** modeled on the grid claim:
`Device.claimed_by_motd_config_id` removes a device from solo rotation while
the session is active; grids and MOTD respect each other's claims (whichever
claim exists first wins, the other pauses/409s).

- **Start**: manually (`POST /api/motd/display` / "Display now") or by the
  daily schedule (time-of-day + weekday mask in an IANA timezone). Each
  participating device gets its first assigned part pushed immediately.
- **Part rotation**: a device with several assigned parts cycles through
  them at its normal refresh interval, driven by the rotation loop's MOTD
  tick. Single-part devices are parked (no wasteful e-ink refreshes).
- **End**: after `display_duration_seconds`, or — when that is null — only
  via manual release (`POST /api/motd/release`). Released devices rejoin
  normal rotation immediately.

Generation runs ahead of display time (`generation_lead_minutes`, default
60) so the scheduled display pushes an already-rendered message; the
"Generate now" button does the same on demand. Rendered screens are plain
exact-size JPEGs under `motd/{message_id}/` in the bucket, pushed through
the ordinary MQTT `DisplayCommand` flow — controllers need no MOTD
awareness. The newest 7 messages are retained; older rows and their S3
objects are pruned after each generation.

## Data model

All in `packages/shared/.../models/motd.py` (migration 0015):

- `motd_configs` — singleton config + live session state
  (`active_message_id`, `active_since`, `active_expires_at`).
- `motd_device_assignments` — per-device ordered part list +
  `rotation_index` (session state).
- `motd_messages` — generated story fields, status
  (`generating|ready|failed`), source URL/title.
- `motd_screens` — one row per rendered part × panel size. Deliberately
  **not** `images` rows, so MOTD content never leaks into FIFO rotation.

## API

All under `/api/motd`:

| Endpoint | Purpose |
|----------|---------|
| `GET /config` | Config incl. assignments (lazily creates the singleton) |
| `PUT /config` | Partial update; `assignments` replaces the whole list |
| `POST /generate` | 202 + task id (shared GenAI task registry) |
| `POST /display` | Start session; 409 without a ready message |
| `POST /release` | End session |
| `GET /status` | Active state + per-device current part |
| `GET /messages/latest` | Latest message with screens (`null` when none) |
| `GET /messages` | Recent messages, newest first |

## Operator UI

The "Message of the day" tab on the GenAI page (`/genai?tab=motd`): actions
bar (Generate now / Display now / Release + live status), the story prompt
with reset-to-default, source mode and image preset pickers, per-device
part assignment chips (selection order = rotation order), daily schedule
(time, timezone, weekdays, generation lead) and duration ("show until
released" or minutes), plus a preview of the latest generated message with
rendered screen thumbnails. It shares the prompt presets (the "Prompt
library" tab) and the "Recent generations" task list with the Images tab.

## Requirements

`API_GEMINI_API_KEY` must be set — both story text and illustration use the
Gemini API (text model per config `text_model_name`, image model from the
selected prompt preset). Fonts for the text screens (DejaVu Sans, bundled)
and QR rendering (`qrcode`) ship with the API package.
