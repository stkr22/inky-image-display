listener 1883
allow_anonymous true

# MQTT-over-WebSockets listener (path defaults to /mqtt for paho/aiomqtt).
# Useful for testing the API/controller transport=websockets code path
# without needing a TLS-terminating ingress in the way.
listener 9001
protocol websockets

persistence false
log_dest stdout
